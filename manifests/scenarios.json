{
  "schema_version": 1,
  "description": "Benchmark scenario-command matrix for all comparators",
  "comparators": [
    "ag",
    "rust-ag",
    "rg",
    "ugrep"
  ],
  "corpus_manifest_hash": "d2b70aa3de31f0f8fa88e307d83a45a5eeac65d2b030226a8f152b17fa8467f3",
  "query_manifest_hash": "644958b7d4abfa6291abbd1585003208a04f1e890db83d81f1d02ae7c573b71d",
  "manifest_hash": "740b9dab2ab619fd0a5836e8895d45d4e5c49cab1ecaf1a0c5a3c468340aff63",
  "scenario_count": 31,
  "scenarios": [
    {
      "id": "literal-simple",
      "description": "Simple literal search across repo",
      "query_id": "q-literal-simple",
      "corpus": ".",
      "groups": [
        "smoke",
        "core-ignore-recursion"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "literal-word",
      "description": "Common word literal search",
      "query_id": "q-literal-word",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "literal-rare",
      "description": "Rare literal (few matches, measures traversal)",
      "query_id": "q-literal-rare",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "literal-nomatch",
      "description": "Literal with zero matches (pure traversal overhead)",
      "query_id": "q-nomatch",
      "corpus": ".",
      "groups": [
        "smoke",
        "core-ignore-recursion"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "regex-simple",
      "description": "Simple alternation regex search",
      "query_id": "q-regex-simple",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "regex-class",
      "description": "Character-class identifier pattern",
      "query_id": "q-regex-class",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "regex-complex",
      "description": "All-caps underscore identifiers",
      "query_id": "q-regex-complex",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "literal-nocase",
      "description": "Case-insensitive literal search",
      "query_id": "q-literal-nocase",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -i {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -i {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -i {pattern} {corpus}",
        "ugrep": "ugrep --color=never -i {pattern} {corpus}"
      }
    },
    {
      "id": "context-before-after",
      "description": "Search with context lines (-B2 -A2)",
      "query_id": "q-literal-simple",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -B2 -A2 {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -B2 -A2 {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -B2 -A2 {pattern} {corpus}",
        "ugrep": "ugrep --color=never -B2 -A2 {pattern} {corpus}"
      }
    },
    {
      "id": "count-matches",
      "description": "Count matching lines (--count)",
      "query_id": "q-literal-word",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity --count {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity --count {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --count {pattern} {corpus}",
        "ugrep": "ugrep --color=never --count {pattern} {corpus}"
      }
    },
    {
      "id": "files-with-matches",
      "description": "List files with matches (-l)",
      "query_id": "q-literal-word",
      "corpus": ".",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -l {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -l {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -l {pattern} {corpus}",
        "ugrep": "ugrep --color=never -l {pattern} {corpus}"
      }
    },
    {
      "id": "literal-src-only",
      "description": "Literal search restricted to src/ directory",
      "query_id": "q-literal-word",
      "corpus": "src",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "edge-ignore-source",
      "description": "Search in ignore-source fixture to verify ignore semantics",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/ignore-source",
      "groups": [
        "edge-cases",
        "core-ignore-recursion"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "edge-hidden-files",
      "description": "Search with --hidden flag in hidden-files fixture",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/hidden-files",
      "groups": [
        "edge-cases",
        "core-ignore-recursion"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity --hidden {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity --hidden {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --hidden {pattern} {corpus}",
        "ugrep": "ugrep --color=never --hidden {pattern} {corpus}"
      }
    },
    {
      "id": "edge-binary-files",
      "description": "Search in binary-files fixture to verify binary detection",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/binary-files",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      },
      "groups": [
        "core-edge-cases",
        "edge-cases"
      ]
    },
    {
      "id": "edge-symlink-traversal",
      "description": "Search following symlinks in symlink-traversal fixture",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/symlink-traversal",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -f {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -f {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -L {pattern} {corpus}",
        "ugrep": "ugrep --color=never -L {pattern} {corpus}"
      },
      "platform_skip": {
        "condition": "platform.system() == 'Windows'",
        "reason": "Symlink creation requires elevated privileges on Windows"
      },
      "groups": [
        "core-edge-cases",
        "edge-cases"
      ]
    },
    {
      "id": "edge-one-device",
      "description": "Search with one-device restriction in one-device fixture",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/one-device",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity --one-device {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity --one-device {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      },
      "platform_skip": {
        "condition": "not cross_device_available",
        "reason": "Cross-device mount point not available on this platform"
      },
      "groups": [
        "core-edge-cases",
        "edge-cases"
      ]
    },
    {
      "id": "edge-large-file",
      "description": "Search in large-file fixture to verify large-file handling",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/large-file",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      },
      "groups": [
        "core-edge-cases",
        "edge-cases"
      ]
    },
    {
      "id": "edge-zero-length-regex",
      "description": "Zero-length regex search to verify safe handling",
      "query_id": "q-edge-zero-len",
      "corpus": "tests/edge-cases/zero-length-regex",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      },
      "groups": [
        "edge-cases"
      ]
    },
    {
      "id": "edge-max-count",
      "description": "Search with --max-count in max-count fixture",
      "query_id": "q-edge-needle",
      "corpus": "tests/edge-cases/max-count",
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity --max-count=3 {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity --max-count=3 {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --max-count=3 {pattern} {corpus}",
        "ugrep": "ugrep --color=never --max-count=3 {pattern} {corpus}"
      },
      "groups": [
        "core-edge-cases",
        "edge-cases"
      ]
    },
    {
      "id": "core-matching-smart-case-lower",
      "description": "Smart-case with all-lowercase pattern (case-insensitive)",
      "query_id": "q-case-smart-lower",
      "corpus": "tests/edge-cases/case-sensitivity",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --smart-case {pattern} {corpus}",
        "ugrep": "ugrep --color=never --smart-case {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-smart-case-upper",
      "description": "Smart-case with uppercase pattern (case-sensitive)",
      "query_id": "q-case-smart-upper",
      "corpus": "tests/edge-cases/case-sensitivity",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --smart-case {pattern} {corpus}",
        "ugrep": "ugrep --color=never --smart-case {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-case-flag-s-i",
      "description": "Case-flag precedence: -s -i (last wins → insensitive)",
      "query_id": "q-case-smart-lower",
      "corpus": "tests/edge-cases/case-sensitivity",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -s -i {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -s -i {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -i {pattern} {corpus}",
        "ugrep": "ugrep --color=never -i {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-case-flag-i-s",
      "description": "Case-flag precedence: -i -s (last wins → sensitive)",
      "query_id": "q-case-smart-lower",
      "corpus": "tests/edge-cases/case-sensitivity",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -i -s {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -i -s {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -s {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-multiline-span",
      "description": "Multiline regex spanning two lines",
      "query_id": "q-multiline-span",
      "corpus": "tests/edge-cases/multiline",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never --multiline {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-nomultiline",
      "description": "Nomultiline: cross-line pattern should not match",
      "query_id": "q-multiline-span",
      "corpus": "tests/edge-cases/multiline",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity --nomultiline {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity --nomultiline {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-word-boundary",
      "description": "Word-boundary matching with -w",
      "query_id": "q-word-boundary",
      "corpus": "tests/edge-cases/word-boundary",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -w {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -w {pattern} {corpus}",
        "rg": "rg --no-heading --color=never -w {pattern} {corpus}",
        "ugrep": "ugrep --color=never -w {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-literal-escape",
      "description": "Literal mode escapes regex metacharacters",
      "query_id": "q-word-boundary",
      "corpus": "tests/edge-cases/word-boundary",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity -Q \"foo.bar\" {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity -Q \"foo.bar\" {corpus}",
        "rg": "rg --no-heading --color=never -F \"foo.bar\" {corpus}",
        "ugrep": "ugrep --color=never -F \"foo.bar\" {corpus}"
      }
    },
    {
      "id": "core-matching-zero-len-x-opt",
      "description": "Zero-length optional pattern x? completes safely",
      "query_id": "q-zero-len-x-opt",
      "corpus": "tests/edge-cases/zero-length-regex",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    },
    {
      "id": "core-matching-zero-len-dot-star",
      "description": "Zero-length .* pattern completes safely with multiline",
      "query_id": "q-zero-len-dot-star",
      "corpus": "tests/edge-cases/zero-length-regex",
      "groups": [
        "core-matching"
      ],
      "commands": {
        "ag": "ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rust-ag": "rust-ag --nocolor --workers=1 --parallel --noaffinity {pattern} {corpus}",
        "rg": "rg --no-heading --color=never {pattern} {corpus}",
        "ugrep": "ugrep --color=never {pattern} {corpus}"
      }
    }
  ]
}
